/**
 *  Exponential Backoff Retries
 */
export declare class BackoffRetry {
    private min;
    private max;
    private k;
    private delay;
    private code;
    retryAttempts: number;
    /**
     * @param min  Initial retry delay
     * @param max  Max retry delay
     * @param k    Increase factor
     */
    constructor(min?: number, max?: number, k?: number);
    /**
     * Delay Retry
     * @param code
     * @returns retryAttempts
     */
    wait(code?: number): Promise<number>;
    /**
     * Reset Retry
     */
    reset(): void;
}
