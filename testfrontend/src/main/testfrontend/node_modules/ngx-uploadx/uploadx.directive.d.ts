import { ElementRef, Renderer2, OnInit, OnDestroy, EventEmitter } from '@angular/core';
import { UploadxOptions, UploadxControlEvent } from './interfaces';
import { UploadxService } from './uploadx.service';
export declare class UploadxDirective implements OnInit, OnDestroy {
    private elementRef;
    private renderer;
    private uploadService;
    listenerFn: () => void;
    uploadxState: EventEmitter<{}>;
    uploadx: UploadxOptions;
    uploadxAction: UploadxControlEvent;
    constructor(elementRef: ElementRef, renderer: Renderer2, uploadService: UploadxService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    fileListener: () => void;
}
